# Open WebUI Docker Container for MLX Sharding
FROM ghcr.io/open-webui/open-webui:main

# Set environment variables for OpenAI API endpoint
ENV OPENAI_API_BASE_URLS=http://host.docker.internal:8080/v1
ENV OPENAI_API_KEYS=dummy

# Open WebUI runs on port 8080 by default
EXPOSE 8080
